# Copie este arquivo para .env e preencha.
# Em dev local, aponta pro backend rodando na sua máquina.
# Em produção (Vercel), aponta pra URL pública da API (Render).

VITE_API_URL=http://localhost:5033